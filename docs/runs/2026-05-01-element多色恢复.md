# Element-level 多色恢复（保留 layout）

日期：2026-05-01
范围：纯视觉调色，element-level 多色（恢复 figma 8f032d0 风），底色保持纯净不拼接

## 任务背景
- 第一次尝试"亮色拼接"（左暖米/中白/右浅蓝） → 用户 reject
- 第二次"A 风极简"（纯白 + 单一紫色） → 用户嫌"颜色太少"
- 本次：底色单一干净（白 / 暖白），但 element / 装饰 / chip / icon / 头像 / category 多色化（figma 风）

## 改了的文件
1. `src/styles/theme.css` — 加 7-color category ramp + 7 种状态 chip 多色 token
2. `src/app/components/CategoryBadge.tsx` — 恢复 7 色 badge（浅底 + 深字 + 彩色 dot + glow）
3. `src/app/components/PostCard.tsx` — 加 category 染色左 stripe（3px subtle）+ category 染色 hover wash + category 染色报酬 chip + tag hover 变 category 色
4. `src/app/components/layout/Sidebar.tsx` — category 菜单 icon 用 category 自己的颜色（active 100%，未 active 78% opacity）；matches badge 改通知红
5. `src/app/components/layout/RightPanel.tsx` — active members 头像 4 种独立色；trending 数字 badge 4 种 rank 色（rose→橙→金→indigo）；Brain OS 已连接 chip 改 status-open 绿；Brain OS 优先级行加彩色 dot + 数字染色
6. `src/app/pages/HomePage.tsx` — 主页 hero "欢迎回来" 改紫色渐变文字（deep indigo → purple → light violet）；onboarding step icon 3 步分别 indigo / 绿 / rose 不同颜色

## CategoryBadge 多色映射（7 种 category）

| Category | bg | text | dot | label |
|---|---|---|---|---|
| all（全部） | rgba(100,116,139,.10) | #475569 | #64748B | 全部 |
| jobs（职位） | rgba(99,102,241,.10) | #3730A3 | #6366F1 | 职位 |
| projects（项目） | rgba(139,92,246,.10) | #5B21B6 | #8B5CF6 | 项目 |
| marketplace（二手） | rgba(249,115,22,.10) | #C2410C | #F97316 | 二手 |
| skills（技能） | rgba(34,197,94,.10) | #15803D | #22C55E | 技能 |
| housing（租房） | rgba(20,184,166,.10) | #0F766E | #14B8A6 | 租房 |
| events（活动） | rgba(244,63,94,.10) | #BE123C | #F43F5E | 活动 |

## Agent 头像多色策略
- `post.author.avatarColor` 在 mockData.ts 已是 8 种独立深色（`#1A1A2E`/`#2D4A22`/`#3D2B1F`/`#1F2D4A`/`#2A1F4A`/`#4A2A2A`/`#1E3A2A`/`#3A1E3A`），不动数据
- RightPanel 的 activeUsers 4 个头像直接改成 4 种鲜亮色（indigo/orange/green/rose），区别度高

## 状态 chip / 按钮渐变 / 其他多色处
- **状态 chip 多色 token**（theme.css）：open（绿）/ pinned（紫）/ closed（slate）/ expired（stone）/ new（蓝）/ hot（rose）/ featured（金） — 后续 PostDetail / PostCard 可按需引用
- **Brain OS 已连接 chip**：`status-open` 绿色（替代灰色）
- **Brain OS 优先级行**：P0 rose dot / P1 橙 dot / P2 绿 dot；count > 0 时数字也染色
- **trending 数字 badge**：rank 1-4 分别 rose / 橙 / 金 / indigo
- **Sidebar matches 通知 badge**：改成实心红 `#DC2626` + 白字 + subtle 红 shadow
- **Sidebar category icon**：每个 category icon 用自己的颜色（active 100%、未 active 78% opacity）+ active stripe 改用 category 自色
- **CTA 紫色渐变**：保持原有 `linear-gradient(180deg, #5B52EA 0%, #4F46E5 50%, #4338CA 100%)` + inset highlight
- **HomePage hero 文字渐变**：`linear-gradient(135deg, #1E0A3C 0%, #4F46E5 55%, #8B5CF6 100%)` + WebkitBackgroundClip text
- **HomePage onboarding step icon**：3 步分别 indigo / 绿 / rose 三种 chip

## 没动什么（边界）
- 三栏 layout（Sidebar `#FCFCFD` / Main `#FAFAFB` / RightPanel `#FFFFFF`）底色全部纯净，**未拼接**
- LoginPage 单栏白底未变（用户上轮选定）
- 左下账号区位置未变（Sidebar 底部）
- TopBar 右上仍清空（已登录时不显示账号块）
- 所有 import / state / handler / props / type / route / data 文件全部未动
- JSX 结构：仅 PostCard 内部加了一个 4px 染色 stripe `<div>`（prompt step 2.3 显式要求加边/角标），其余文件 0 element 增删
- PostCard `pl-6`（增加左 padding 给 stripe 留空间）匹配 figma 8f032d0 原版

## Vite + uvicorn 状态
- Vite HMR：✅ 全部干净（最新 timestamp 4:42:15 PM，最后是 Sidebar.tsx）
- 无 compile error / 无 import error
- localhost:5173 → HTTP 200
- 唯一警告：`getCategoryLabel` Fast Refresh 提示（pre-existing dual export，与本次无关）

## 已知问题
- 没跑 Playwright（环境无 playwright 包，且每次截图脚本会改环境）
- mockData.ts 的 `avatarColor` 字段值偏深（`#1A1A2E` 等），看着可能不够鲜亮，但未改 data 文件（数据层 forbidden）
- 状态 chip token 加好了但 PostCard / PostDetailPanel 还没引用（数据层 mockData 也没 status 字段，没合适触发点）— 留作后续

## 给主 agent 的下一步
浏览器开 http://localhost:5173/ 看主页 PostCard 列表：每张卡片左侧 3px 染色细条按 category 上色、CategoryBadge 7 种独立浅底深字色 + 彩色 dot、hover 时卡片有极轻 category 色 wash、报酬 chip 用 category 色；侧栏 7 个 category icon 各自彩色；右栏 trending 数字 badge / Brain OS 优先级 dot / Active Members 头像都多色。底色仍纯白/暖白干净。
