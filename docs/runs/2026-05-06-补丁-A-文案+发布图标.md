# 补丁 A — 文案修订 + 发布图标渐变化

日期：2026-05-06
范围：2 个小改，2 个文件
状态：PASS

## 任务 1 — "魔法链接" → "登录链接"（用户可见 UI 文案）

### 全量调研结果
grep `魔法|Magic|magic` 结果（src/ 全量）：

| 位置 | 类型 | 是否用户可见 | 处理 |
|------|------|-------------|------|
| LoginPage.tsx:386 | h2 标题文本 | ✅ 是 | **改：魔法链接已发送 → 登录链接已发送** |
| LoginPage.tsx:34 | useState 变量名 magicLinkUrl | ❌ | 不动（变量名）|
| LoginPage.tsx:32 | step 联合类型 'magic-sent' | ❌ | 不动（state 字面量）|
| LoginPage.tsx:51,291,367 | 代码注释 | ❌ | 不动（注释）|
| LoginPage.tsx:44,58,59 | dev_magic_link API 字段 / 函数名 | ❌ | 不动（API 契约）|
| MagicVerifyPage.tsx:3,20,33 | 路由组件名 / import / 函数调用 | ❌ | 不动（路由+API）|
| translations.ts | （无匹配）| — | 无需改 |

**仅改动 1 处用户可见文案**。其他都是 state 字面量、API 字段名、变量名、注释 —— 按指令"不要改后端 endpoint 路径"。

### 改动
- `src/app/pages/LoginPage.tsx:386` — `魔法链接已发送` → `登录链接已发送`

### 已经合规（无需改动）
- LoginPage.tsx:411 文案"链接 15 分钟内有效。在飞书 / 邮箱也会同时收到。" 已经只用"链接"
- LoginPage.tsx:399 "DEV 模式：直接点这里登录" 没有"魔法"
- 没有英文 "Magic link..." 用户可见标签

## 任务 2 — TopBar 发布按钮：立体 → 平面紫色渐变

### 当前风格（改前）
- `src/app/components/layout/TopBar.tsx:196-213`
- 立体 figma 风：`linear-gradient(180deg, #5B52EA 0%, #4F46E5 50%, #4338CA 100%)` 三色纵向渐变
- inset 反光：`inset 0 1px 0 rgba(255,255,255,0.18)` （顶部白色高光，立体感来源）
- 阴影：`0 1px 2px rgba(79,70,229,0.22)`

### 改后风格
- 135deg 双色平面渐变：`linear-gradient(135deg, #4F46E5 0%, #7C3AED 100%)`
- 去掉 inset 反光，加 `border: none`
- 阴影：`0 4px 12px rgba(79, 70, 229, 0.25)` （subtle）
- hover：渐变色更深 + `translateY(-1px)` 微浮起，阴影略加重
- 圆角保持 `rounded-xl`（与 TopBar 其他按钮一致）
- icon 保持 lucide `Plus`（白色，14×14）
- 按钮文案 `t('action.post')` 不动

### 改动
- `src/app/components/layout/TopBar.tsx:196-220` — Post CTA 按钮 style 块整体替换

## 验证

| 项 | 结果 |
|---|------|
| TypeScript（项目无本地 tsc，跳过）| 跳过（预期，依赖 Vite transpile）|
| Vite HMR LoginPage.tsx | curl 200 OK，模块转译干净 |
| Vite HMR TopBar.tsx | curl 200 OK，模块转译干净 |
| Vite dev server (5173) | 运行中（PID 95389）|
| 功能 (state / handler / api / route) | 未触碰，仅文案+样式 |
| 新依赖 | 无 |
| 其他文件 | 未碰 |

## 硬约束遵守
- ✅ 不 commit / push
- ✅ 不破坏功能
- ✅ 不 install 新依赖
- ✅ 不碰其他文件
- ✅ 紫色 brand `#4F46E5 → #7C3AED` 保留
- ✅ PingFang SC 字体未改动
- ✅ dev_magic_link API 字段名保留

## 浏览器手动验证（用户侧）
1. http://localhost:5173/login 输邮箱提交 → "登录链接已发送" 文案
2. 登录后看 TopBar 右上角发布按钮 → 双色平面渐变紫，hover 微浮起 -1px

## 下一步
请用户在浏览器手动看一眼两处效果；若发布按钮 hover 浮起感太强可改为不动。
