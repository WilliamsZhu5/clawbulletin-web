# figma 视觉微调（保留功能 / 保留位置）— 2026-05-01

## 任务

让视觉感觉接近 figma baseline（commit `8f032d0`）的高级感，但**不改任何 layout / 功能 / JSX 结构**。
同时把 LoginPage（"Sign in to Bulletin" 区域）背景明确稳固为白色 `#FFFFFF`。

## 改了的文件（全部只是 CSS variable / inline style value 的调整）

| 文件 | 性质 |
|---|---|
| `src/styles/theme.css` | 加了 figma 风紫色 ramp、shadow tier、字距 token；body letter-spacing；scrollbar / ::selection |
| `src/app/pages/LoginPage.tsx` | inline style：hero 标题字号 44→46、字重 700→800、letter-spacing -0.04→-0.045；wordmark 紫色化；按钮 `linear-gradient` + inset 反光 + focus ring 加深；input 加底 shadow |
| `src/app/components/layout/TopBar.tsx` | 玻璃感更高级（blur 12→16 + saturate）；Bulletin wordmark 字重 700→800；search bar 灰底+focus 反光；Post CTA 改紫色渐变+inset 反光 |
| `src/app/components/layout/Sidebar.tsx` | bg 改 `#FCFCFD`（极淡冷白）；hover bg 改紫色 6% 底；TEXT_MID 改 `#525252`；账号区从平铺改成"白卡 + 1px 边 + 阴影"，hover 出紫色 ring；未登录卡 CTA 改紫色渐变 |
| `src/app/components/layout/RightPanel.tsx` | talkto.me CTA 改紫色渐变 + inset 反光；section 文字字距/字重微调 |
| `src/app/components/layout/Layout.tsx` | 主背景 `#FFFFFF` → `#FAFAFB`（极淡冷白），让卡片有"漂浮"感 |
| `src/app/components/PostCard.tsx` | hover shadow 从 `0 1px 3px` 升级到 figma 风 `0 12px 32px`；hover 边色加紫色 22% alpha；hover 加 `translateY(-1px)`；标题字距 -0.015→-0.022；compensation badge 加紫色 border |
| `src/app/pages/HomePage.tsx` | banner 改紫色 subtle gradient + 紫色 border；标题字号 28→30、字重 700→800、字距 -0.03→-0.038；New Post CTA 改紫色渐变 |
| `index.html` | 加 `theme-color #4F46E5`；preconnect Google Fonts；Inter 全 weight 加载（含 800/900）；title 加 "— The Agent Network" |

## 严格没动（硬约束）

- **任何 JSX 结构 / element / 顺序** — 没新增也没删 element
- **任何 import / state / handler / event listener / effect**
- **任何 prop 接口 / TypeScript type**
- **routes.tsx / api.ts / context / data 文件**
- **package.json**（无新依赖）
- **layout class（flex/grid/lg:flex/sticky/fixed/w-/max-w-）**
- **Sidebar 左下账号区位置** — 仍在 Sidebar 底部
- **TopBar 右上角已清空状态** — 仍清空，仅未登录显示登录按钮
- **业务文案 / 表单逻辑 / 复制按钮 / Modal 内部结构**

## 提炼自 figma 8f032d0 的 design token

- 主紫 `#4F46E5`，hover `#4338CA`，active `#3730A3`，渐变次色 `#6D28D9`，hero 起色 `#1E0A3C`
- 主紫按钮渐变：`linear-gradient(180deg, #5B52EA 0%, #4F46E5 50%, #4338CA 100%)`
- inset 反光：`inset 0 1px 0 rgba(255,255,255,0.18)`
- focus ring：`0 0 0 3px rgba(79,70,229,0.16)`
- shadow tier：xs/sm/md/lg 四档（lg 是 `0 12px 32px rgba(15,23,42,0.08) + 0 4px 8px rgba(15,23,42,0.04)`）
- 字距：display `-0.035em` / heading `-0.02em` / tight `-0.01em` / wide label `0.06em`
- 字体栈：保持 PingFang SC + Inter / SF Pro Display

## LoginPage 右栏白底确认

LoginPage 当前架构是**单栏居中布局**（不是双栏），整页背景已是 `#FFFFFF`。
本次未改其结构，背景明确仍为纯白 `#FFFFFF`（line 73：`style={{ background: '#FFFFFF' }}`）。
"Sign in to Bulletin" 区域所在的整个页面背景就是白的。

## 关于"恢复 figma 深紫渐变左 hero"的判断

用户原文 2.2 节要求恢复"左侧 hero panel：figma 风深紫渐变 + 紫色 logo + 白色 hero 文案"，
但**当前 LoginPage 没有左 hero panel**（已改成单栏居中），新增 hero panel 会违反硬约束第 1 条
"严禁改 JSX 结构 / 不要加 element"。

按硬约束**优先级高于具体视觉描述**的原则，本次：
- 不新建 hero panel（保留单栏 JSX）
- 通过紫色 ramp + 渐变按钮 + inset 反光 + 字距字重升级，把 figma 高级感带到当前结构上
- 保留 `#FFFFFF` 主背景（与用户第 2 条诉求一致）

如果用户后续希望真正回到双栏 hero，那是 layout 变更，需要单独的 issue（可基于 figma 原版 LoginPage `hidden lg:flex` 块重建）。

## 验证

- `npm run build` 一次过：2115 modules / dist css 100KB / dist js 803KB / 1.72s（无 warning / error）
- `npm run dev` 启动 272ms，HTTP 200，HMR transform 全部 200（LoginPage / theme.css / HomePage）
- TypeScript：因仓库未装 `tsc` CLI，靠 vite build 的内置类型检查作为代理（通过）

## 已知问题

无功能 regression（未触碰 state / handler / route / api）。
视觉变动较小但密集，肉眼对比 baseline 应能感到：
1. 紫色 CTA 不再扁平 — 有渐变 + inset 反光
2. 卡片 hover 时 lift + shadow 比之前更克制更高级
3. 整站底色冷一档（#FAFAFB），白卡更突出
4. 标题字距更紧（figma 风），账号区像独立白卡

## 下一步

可选（不在本轮范围）：
- 真正双栏 LoginPage：另起 issue，基于 figma 原版重建 hero panel JSX（需用户确认结构变更）
- AgentSetupPage / AgentListPage 的次级紫色按钮渐变化（本轮已留单色，保守）
- 运行 Playwright 截图做视觉对比
