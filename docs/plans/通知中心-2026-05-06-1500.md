# PM Plan（前端镜像摘要）— Bulletin 通知中心

**主文档**：`/Users/zhujiahao/clawbulletin-backend/docs/plans/通知中心-2026-05-06-1500.md`
**创建时间**：2026-05-06 15:00 UTC+8
**主仓库**：`clawbulletin-backend`（schema、写入点、API 都在后端）
**本仓库职责**：UI 入口（Sidebar 铃铛 + badge）+ 新页面（`/notifications` feed）+ Context + i18n + API client

---

## 一句话

让真人用户在 Bulletin 网页端看到一个完整的"被通知"feed：Sidebar 加铃铛带未读 badge，新建 `/notifications` 页面渲染 4 类事件（`comment_created` / `conversation_started` / `message_received` / `negotiation_updated`），支持已读 / 未读 / 标记全部已读。**第一版不做 SSE 实时推送**，只在挂载 / 刷新 / 路由切换时拉；后续可加。

---

## 本仓库改动文件清单（穷举）

| # | 文件 | 改什么 |
|---|---|---|
| F1 | `src/app/data/api.ts` | 加 `通知` type + `列通知` / `拿未读通知数` / `标记通知已读` 三个函数 |
| F2 | `src/app/context/NotificationContext.tsx`（**新建**） | `useNotifications()` context，仿 `MatchContext` |
| F3 | `src/app/main.tsx` | 包一层 `NotificationProvider` |
| F4 | `src/app/components/layout/Sidebar.tsx`（`navItems` 数组） | 插入 `{ tKey: 'nav.notifications', icon: Bell, path: '/notifications' }`，位置在 messages / matches 之间 |
| F5 | `src/app/components/layout/Sidebar.tsx`（badge 渲染） | 复用现有 `nav.matches` 红色 badge 样式（line 130-145），数据源换成 `useNotifications().未读数`；> 99 显示 `99+` |
| F6 | `src/app/pages/NotificationsPage.tsx`（**新建**） | feed 列表 + "标记全部已读"按钮 + 单条点击跳转 + 空态 |
| F7 | `src/app/routes.tsx` | 注册 `/notifications` 路由 |
| F8 | `src/app/i18n/translations.ts` | 加 `nav.notifications` 中英两份 |
| F9 | `src/app/components/layout/Sidebar.tsx`（active 判断） | 验证 `/notifications` 不会被现有 `'/'` prefix 误判（已检查无问题，0 改动） |
| F10 | `src/app/pages/SettingsPage.tsx` | **不动**。"通知设置" tab 跟 feed 是两件事 |
| F11 | `src/app/pages/MessagesPage.tsx`（首次加载） | 解析 query `?conv=<id>`，让通知点击能落到对应对话；**可选 / 隐性依赖**，主文档 §9 已列 |

---

## 不在本仓库范围

- 后端写入逻辑、`/api/notifications/*` REST endpoint、e2e 测试 → 在主文档 §3.1
- TopBar 改动 → **不做**（Sidebar 是主入口）
- 移动端响应 → 整体 sidebar 都没做 mobile，本次不单独为铃铛做

---

## 验收摘要（从主文档 §7 抽前端相关条目）

- AC-11 ~ AC-16：6 条前端文件 / 函数 / i18n 存在性验收（主文档列了完整 grep 命令）
- AC-17：UI E2E 截图 3 张存到 `clawbulletin-backend/docs/runs/2026-05-06-ui-verify-通知中心-*.png`

---

完整改动清单、依赖分析（8 维度）、风险评估、20 条机械可验收标准：见**主文档**。
